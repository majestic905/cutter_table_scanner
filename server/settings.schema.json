{
  "definitions": {
    "point":  {
      "type": "array",
      "items": [
        { "type": "integer", "minimum": 0 },
        { "type": "integer", "minimum": 0 }
      ]
    },

    "projection_points": {
      "type": "object",
      "required": ["bottom_left", "bottom_right", "top_right", "top_left"],
      "properties": {
        "bottom_left": { "$ref": "#/definitions/point" },
        "bottom_right": { "$ref": "#/definitions/point" },
        "top_right": { "$ref": "#/definitions/point" },
        "top_left": { "$ref": "#/definitions/point" }
      }
    },

    "blank_camera": {
      "type": "object",
      "required": ["type", "projection_points", "projected_image_size", "table_section_size"],
      "properties": {
        "type": { "type": "string" },
        "projection_points": { "$ref": "#/definitions/projection_points" },
        "projected_image_size": { "$ref": "#/definitions/point" },
        "table_section_size": { "$ref": "#/definitions/point" }
      }
    },

    "real_camera": {
      "type": "object",
      "required": ["type", "projection_points", "projected_image_size", "table_section_size", "maker", "model", "serial_number"],
      "properties": {
        "type": { "type": "string" },
        "projection_points": { "$ref": "#/definitions/projection_points" },
        "projected_image_size": { "$ref": "#/definitions/point" },
        "table_section_size": { "$ref": "#/definitions/point" },
        "maker": { "type": "string" },
        "model": { "type": "string" },
        "serial_number": { "type": "string" }
      }
    },

    "dummy_camera": {
      "type": "object",
      "required": ["type", "projection_points", "projected_image_size", "table_section_size", "maker", "model"],
      "properties": {
        "type": { "type": "string" },
        "projection_points": { "$ref": "#/definitions/projection_points" },
        "projected_image_size": { "$ref": "#/definitions/point" },
        "table_section_size": { "$ref": "#/definitions/point" },
        "maker": { "type": "string" },
        "model": { "type": "string" },
      }
    }
  },

  "type": "object",
  "required": ["cameras"],
  "properties": {
    "cameras": {
      "type": "object",
      "required": ["LU", "LL", "RL", "RU"],
      "properties": {
        "LU": { "$ref": "#/definitions/camera" },
        "LL": { "$ref": "#/definitions/camera" },
        "RL": { "$ref": "#/definitions/camera" },
        "RU": { "$ref": "#/definitions/camera" }
      }
    }
  }
}