{
  "definitions": {
    "point":  {
      "type": "array",
      "items": [
        { "type": "integer", "minimum": 0 },
        { "type": "integer", "minimum": 0 }
      ]
    },

    "camera": {
      "type": "object",
      "required": ["maker", "model", "type", "projection_points", "projected_image_size", "real_size"],
      "properties": {
        "maker": { "type": "string" },
        "model": { "type": "string" },
        "usb_port": { "type": "string" },
        "projection_points": {
          "type": "object",
          "required": ["bottom_left", "bottom_right", "top_right", "top_left"],
          "properties": {
            "bottom_left": { "$ref": "#/definitions/point" },
            "bottom_right": { "$ref": "#/definitions/point" },
            "top_right": { "$ref": "#/definitions/point" },
            "top_left": { "$ref": "#/definitions/point" }
          }
        },
        "projected_image_size": { "$ref": "#/definitions/point" },
        "real_size": { "$ref": "#/definitions/point" }
      }
    }
  },

  "type": "object",
  "required": ["cameras"],
  "properties": {
    "cameras": {
      "type": "object",
      "required": ["LU", "LL", "RL", "RU"],
      "properties": {
        "LU": { "$ref": "#/definitions/camera" },
        "LL": { "$ref": "#/definitions/camera" },
        "RL": { "$ref": "#/definitions/camera" },
        "RU": { "$ref": "#/definitions/camera" }
      }
    }
  }
}